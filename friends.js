var numbers = [2, 3, 4, 5, 6, 7, 8, 31];
for (var i = 0; i < numbers.length; i++) {
    if (numbers[i] > 30) {
        console.log(numbers[i]);
    }
}

for (var i = 0; i < numbers.length; i++) {
    if (numbers[i] < 60) {
        console.log(numbers[i]);
    }
}